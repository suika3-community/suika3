Suika3 Plugin Development Reference
===================================

## Folder

Plugins must be stored in the `plugin/<PLUGIN-NAME>/` directory.

## File

Plugin file must be stored to the `plugin/<PLUGIN-NAME>/<PLUGIN-NAME>.pf` file.

## Function

Plugin must define the `plugin_init_<PLUGIN-NAME>()` function.
